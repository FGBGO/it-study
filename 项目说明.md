# IT 学习中心 · 项目说明

这是一个个人 IT 教学网站（Vue 3 + Vite），内置 11 个技能分类的教程和综合测试。
本文档说明项目中**重要文件的位置和作用**，方便日后维护和添加新内容。

---

## 一、怎么运行（日常使用）

| 操作 | 做法 |
|---|---|
| 启动网站 | 双击 `start.bat`，浏览器访问 http://127.0.0.1:5173 |
| 关闭网站 | 双击 `stop.bat` |
| 首次运行前 | 需要安装 Node.js，然后执行一次 `npm install` |
| 发布到 GitHub Pages | 双击 `push-master.bat` 推代码，再双击 `publish-gh.bat` 发布网站 |

> 所有 .bat 脚本执行后窗口都不会自动关闭，方便查看报错。

---

## 二、项目结构总览

```
it-study/
├── index.html              网页入口（几乎不用改）
├── package.json            项目配置：依赖、脚本命令
├── package-lock.json       依赖版本锁定（自动生成，别手改）
├── vite.config.js          Vite 配置：端口、GitHub Pages 的 base 路径
├── .gitignore              Git 忽略规则（node_modules、dist 等）
│
├── start.bat               启动网站
├── stop.bat                停止网站
├── push-master.bat         推送代码到 GitHub
├── publish-gh.bat          发布网站到 GitHub Pages（推荐，纯 Git 方式）
├── deploy-gh.bat           发布网站（一键版：装依赖+构建+发布）
│
└── src/                    源代码（主要工作都在这里）
    ├── main.js             程序入口，创建并挂载 Vue 应用
    ├── App.vue             根组件：顶部导航栏 + 页面区 + 页脚
    │
    ├── router/
    │   └── index.js        路由配置：URL 和页面的对应关系
    │
    ├── views/              页面级组件（一个文件 = 一个页面）
    │   ├── Home.vue        首页：分类卡片入口
    │   ├── Category.vue    分类页：某技能的章节目录
    │   ├── Lesson.vue      课时页：具体一节课的教学内容
    │   └── Quiz.vue        综合测试页：抽题、答题、交卷、看解析
    │
    ├── components/         可复用小组件
    │   ├── CodeBlock.vue       代码块：带标题栏和"复制"按钮
    │   └── SectionRenderer.vue 教学内容渲染器：按类型显示文字/列表/代码/表格/提示框
    │
    ├── data/               ★ 教学内容数据（加新课程主要改这里）
    │   ├── index.js        数据入口：汇总所有分类（新增分类要在这里注册）
    │   ├── quiz.js         综合测试题库（选择题 + 主观题）
    │   ├── html.js         HTML 教程（8 章 30 课）
    │   ├── css.js          CSS 教程（7 章 28 课）
    │   ├── js.js           JavaScript 教程（9 章 35 课）
    │   ├── vue.js          Vue 教程（9 章 31 课）
    │   ├── uniapp.js       小程序开发 uni-app 教程（9 章 30 课）
    │   ├── java.js         Java 教程（9 章 34 课）
    │   ├── springboot.js   Spring Boot 教程（9 章 32 课）
    │   ├── python.js       Python 教程（9 章 33 课）
    │   ├── pg.js           PostgreSQL 教程（9 章 32 课）
    │   ├── git.js          Git 教程（9 章 30 课）
    │   └── maven.js        Maven 教程（8 章 24 课）
    │
    ├── utils/
    │   └── format.js       工具函数
    │
    └── assets/
        └── main.css        全局样式（配色变量、导航栏、卡片、按钮等）
```

---

## 三、核心文件详解

### 1. 配置类

**`package.json`** — 项目说明书
- 记录项目依赖（vue、vite 等）和命令脚本（dev 启动、build 构建）
- 一般不需要手动改，安装新依赖时用 `npm install 包名` 自动更新

**`vite.config.js`** — 构建工具配置
- 设置开发服务器端口 5173（固定端口，stop.bat 靠它找进程）
- `base` 配置：本地运行是 `/`，发布 GitHub Pages 时自动变成 `/it-study/`

**`index.html`** — 网页总入口
- 只包含一个 `<div id="app">`，所有页面内容都由 Vue 动态渲染进去
- 改网站标题（浏览器标签页文字）在这里改 `<title>`

### 2. 框架骨架类

**`src/main.js`** — 程序起点
- 创建 Vue 应用、挂载路由、引入全局样式，然后渲染到 index.html

**`src/App.vue`** — 根组件（整体框架）
- 顶部导航栏：首页 / 前端开发▾ / 后端与数据库▾ / 开发工具▾ / 综合测试
- 下拉菜单的分组逻辑（GROUP_MAP）在这里
- **新增分类时**：要在对应下拉菜单里加一行 `<RouterLink>`

**`src/router/index.js`** — 路由表
- 定义 URL 规则：
  - `/` → 首页
  - `/category/:id` → 分类页
  - `/lesson/:id` → 课时页
  - `/quiz` → 测试页
- 使用 hash 路由（网址带 #），好处是部署到 GitHub Pages 不会出现刷新 404

### 3. 页面类（src/views/）

| 文件 | 作用 | 什么时候改 |
|---|---|---|
| `Home.vue` | 首页，展示所有分类卡片和学习建议 | 改欢迎语、学习路线文案 |
| `Category.vue` | 分类目录页，列出章节和课时 | 一般不用改（自动读取数据） |
| `Lesson.vue` | 课时详情页，渲染一节课的全部内容，带上一课/下一课 | 一般不用改 |
| `Quiz.vue` | 综合测试：选范围→抽题→作答→交卷→成绩与解析 | 改测试规则、样式；新增分类时要加分类名和选项 |

### 4. 组件类（src/components/）

- **`CodeBlock.vue`**：代码展示块，右上角"复制"按钮靠它实现
- **`SectionRenderer.vue`**：教学内容渲染器。数据里每段内容标了类型（text 文字 / list 列表 / code 代码 / table 表格 / tip 提示 / warn 警告），由它决定长什么样

### 5. ★ 数据类（src/data/）—— 添加内容主要碰这里

**`index.js`** — 分类注册入口
- 每个教程文件在这里 import，然后放进 `categories` 数组
- 还提供两个工具函数：`getLessonById`（按 id 找课时）、`getNeighbors`（找上一课/下一课）

**教程文件（html.js / css.js / ... ）** — 每个文件一个技能
- 结构固定：`id` / `name`（全名）/ `short`（徽标简称）/ `color`（主题色）/ `description` / `chapters`（章节）
- 章节 → 课时 → sections（内容段），每节课由若干段组成

**`quiz.js`** — 题库
- 选择题字段：`type:'choice'`、`category`、`question`、`options`（4 个选项）、`answer`（正确选项下标 0~3）、`explanation`（解析）
- 主观题字段：`type:'subjective'`、`category`、`question`、`reference`（参考答案）

---

## 四、常见维护操作

### 添加一个新技能（比如 TypeScript）

1. 复制 `src/data/git.js` 改名为 `typescript.js`，修改里面的 id、name、color、章节内容
2. 在 `src/data/index.js` 顶部 import，并加入 `categories` 数组
3. 在 `src/App.vue` 导航的对应下拉菜单里加一行 `<RouterLink to="/category/typescript">...</RouterLink>`，并在 GROUP_MAP 里加分组映射
4. 在 `src/views/Quiz.vue` 的 `catNames` 和 `rangeOptions` 里加一项，再加一条 `.progress-tag.cat-xxx` 样式
5. 在 `src/data/quiz.js` 末尾追加该分类的题目
6. 更新 `src/views/Home.vue` 首页文案和 `App.vue` 页脚
7. 双击 `start.bat` 验证，没问题后 Git 提交

### 添加一节课

在对应分类的 `src/data/xxx.js` 里，找到章节的 `lessons` 数组，照抄一节课的格式添加即可，**课时 id 必须唯一**（如 `ts-ch1-xxx`）。

### 添加测试题

在 `src/data/quiz.js` 数组里追加题目对象，注意 `category` 要和数据文件的 id 一致。

### 修改网站配色 / 样式

- 全局颜色变量、导航栏、卡片样式：`src/assets/main.css`
- 某个页面独有的样式：对应 .vue 文件底部的 `<style scoped>`

---

## 五、版本与部署备忘

- 用 Git 管理版本，关键节点都有提交记录，可用 `git log --oneline` 查看
- 发布流程：`push-master.bat`（推代码）→ `publish-gh.bat`（发布 dist 到 gh-pages 分支）→ 仓库 Settings → Pages 选 gh-pages 分支
- 发布前必须先构建：脚本会自动执行，手动构建命令是 `set VITE_BASE=/it-study/ && npm run build`
- 线上地址：https://FGBGO.github.io/it-study/

---

## 六、推荐学习顺序

HTML → CSS → JavaScript → Vue → 小程序（uni-app）→（Java → Maven → Spring Boot 或 Python）→ PostgreSQL
Git 从第一天开始用起来（每学完一课提交一次）。
